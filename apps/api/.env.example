# Optional OpenAI Chat Completions integration. Without a key the deterministic fallback is used.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional. Defaults to packages/db/data/evomind.sqlite.
DATABASE_URL=
